Results and errors
==================

What an operation that can fail returns: a value, or a failure whose cause says why there is none. And the exceptions, for code that would rather catch.

From ``<proxima/result.hpp>``, ``<proxima/errors.hpp>``.

.. doxygengroup:: results
   :content-only:
   :members:
   :undoc-members:
